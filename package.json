{
  "name": "SecureBank",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root",
    "dev": "concurrently -n BACK,FRONT -c green,cyan \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
